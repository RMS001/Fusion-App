fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
pydantic>=2.10.0
# Tools (optional at runtime — the corresponding tool reports itself
# unavailable if missing)
ddgs>=9.0.0
trafilatura>=2.0.0
